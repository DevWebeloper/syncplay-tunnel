#!/usr/bin/env python3
"""Launcher. The application itself lives in the syncplay_tunnel package."""
import os
import sys

sys.path.insert(0, os.path.dirname(os.path.abspath(os.path.realpath(__file__))))

from syncplay_tunnel.ui.app import main  # noqa: E402

if __name__ == "__main__":
    main()
